# Builds the module for wxWidgets
!include wxWidgets.yaml
!include rcic-module.yaml
---
- package: wxWidgets Module
  category: TOOLS
  module:
    prereq: 
      - *ModuleCompiler_prereq
    requires:
      - *ModuleCompiler_requires
